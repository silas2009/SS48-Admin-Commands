local Settings = _G.Settings

-- Commands

local commands = {
	{
		Name = "kill",
		Nicknames = {"die"},
		Command = function()
            
		end,
		UnCommand = function()

		end,
	},
}

return commands
